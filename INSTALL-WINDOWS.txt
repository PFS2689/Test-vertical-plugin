Vertical Shorts Plugin for OBS Studio — Windows Install (v1.0.5)
===============================================================

Professional Vertical Streaming Plugin for OBS Studio.

Downloads (ClamAV + Windows Defender scanned before each GitHub release is published):

  Setup.exe (recommended):
    https://github.com/PFS2689/verticalshorts/releases/latest/download/Vertical.Shorts.Plugin.1.0.5.Setup.exe

  Manual zip:
    https://github.com/PFS2689/verticalshorts/releases/latest/download/Vertical-Shorts-Plugin-1.0.5.zip

  Checksums:
    https://github.com/PFS2689/verticalshorts/releases/latest/download/SHA256SUMS.txt

About the Setup.exe
-------------------
This is a standard Inno Setup 6 installer. It installs into your OBS Studio
folder (default C:\Program Files\obs-studio):

  <OBS>\obs-plugins\64bit\obs-shorts-vertical.dll
  <OBS>\data\obs-plugins\obs-shorts-vertical\

Administrator rights (UAC) are required. Setup detects OBS automatically; if
OBS is not found you can browse to the folder that contains bin\64bit\obs64.exe.

Local filename (exact):

  Vertical Shorts Plugin 1.0.5 Setup.exe

Authenticode:
  When signing credentials are configured in CI, production releases are
  Authenticode-signed. Verify with:
    Get-AuthenticodeSignature "Vertical Shorts Plugin 1.0.5 Setup.exe"
  Status should be Valid when signed.

Do NOT disable Windows Defender.

Requirements
------------
- Windows 10/11 64-bit
- OBS Studio 32.2.1 (built and tested against 32.2.1)

Upgrading from a previous version
---------------------------------
You do not need to uninstall first.

1. Download the newer "Vertical Shorts Plugin <version> Setup.exe".
2. Run it. If an older build is detected, choose Upgrade (or Cancel).
3. If OBS is open, choose Close OBS and Continue (or Cancel). Setup will not
   replace the plugin DLL while OBS has it loaded.
4. Finish the wizard. Your destinations, credentials, vertical scenes/sources,
   schedules, and other settings are preserved. No computer restart is required.

The AppId stays the same across every Vertical Shorts Plugin version so Windows
treats each Setup.exe as an upgrade of the same product.

Install with Setup.exe (recommended)
------------------------------------
1. Close OBS Studio completely.
2. Run "Vertical Shorts Plugin 1.0.5 Setup.exe", approve UAC, confirm the OBS
   folder (usually C:\Program Files\obs-studio), and finish the wizard.
3. Start OBS Studio.
4. If OBS shows Tools → Plugin Manager, ensure "Vertical Shorts Plugin" is enabled,
   then restart OBS if you just enabled it.
5. Open View → Docks → Vertical Shorts
   (also Tools → Vertical Shorts).
6. Confirm the main OBS canvas remains unchanged while editing the separate vertical composition.
7. Test Vertical Go Live, Vertical Record, Short Clip, Long Clip, and Settings.
8. Confirm output files save to the selected vertical recording path.

Install from zip
----------------
1. Close OBS Studio completely.
2. Locate your OBS install folder (usually C:\Program Files\obs-studio).
3. Copy:

   zip\obs-shorts-vertical\bin\64bit\obs-shorts-vertical.dll
     → <OBS>\obs-plugins\64bit\obs-shorts-vertical.dll

   zip\obs-shorts-vertical\data\*
     → <OBS>\data\obs-plugins\obs-shorts-vertical\

   Final paths must be:

   C:\Program Files\obs-studio\obs-plugins\64bit\obs-shorts-vertical.dll
   C:\Program Files\obs-studio\data\obs-plugins\obs-shorts-vertical\locale\en-US.ini

4. Start OBS Studio.
5. Enable the plugin in Tools → Plugin Manager if needed, then restart OBS.
6. Open View → Docks → Vertical Shorts.
7. Test Vertical Go Live, Vertical Record, Short Clip, Long Clip, and Settings.

If the dock does not appear
---------------------------
1. Confirm the DLL path above exists.
2. Open Help → Log Files → Current Log and search for "obs-shorts-vertical".
   You should see "Loading Vertical Shorts Plugin" and "Dock registered".
3. Check Tools → Plugin Manager — the plugin must be Enabled.
4. Remove any leftover copy under %APPDATA%\obs-studio\plugins\obs-shorts-vertical
   or %ProgramData%\obs-studio\plugins\obs-shorts-vertical (legacy).

Uninstall
---------
Use Windows Settings → Apps → Vertical Shorts Plugin, or run the Inno Setup
uninstaller. You can also delete:

  <OBS>\obs-plugins\64bit\obs-shorts-vertical.dll
  <OBS>\data\obs-plugins\obs-shorts-vertical\

Uninstall removes plugin binaries only. Scene-collection settings and stored
credentials are left intact so a later reinstall can restore your workspace.

Then restart OBS.

Source
------
https://github.com/PFS2689/verticalshorts
